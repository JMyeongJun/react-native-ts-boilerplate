import React, { useEffect } from 'react';
import DetailPresenter from './DetailPresenter';

const DetailContainer = ({ navigation }: any) => {
  return <DetailPresenter />;
};

export default DetailContainer;
