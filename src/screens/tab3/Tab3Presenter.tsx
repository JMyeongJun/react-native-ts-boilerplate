import React from 'react';

/**
 * ? Local Imports
 */
import * as s from './Tab3Style';

interface Tab3Props {}

const Tab3Presenter: React.FC<Tab3Props> = (props) => {
  return (
    <s.Container>
      <s.Text>Tab3</s.Text>
    </s.Container>
  );
};

export default Tab3Presenter;
